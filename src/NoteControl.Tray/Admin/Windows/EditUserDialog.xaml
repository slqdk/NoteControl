<Window x:Class="NoteControl.Tray.Admin.Windows.EditUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit user"
        Width="420" Height="280"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Username:" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="UsernameText"
                   VerticalAlignment="Center" Margin="0,4" Padding="6,4"
                   Foreground="#444"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Email:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="EmailBox" Margin="0,4" Padding="6,4"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Role:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="RoleBox" Margin="0,4" Padding="6,4">
            <ComboBoxItem Content="user"/>
            <ComboBoxItem Content="admin"/>
        </ComboBox>

        <Label Grid.Row="3" Grid.Column="0" Content="Status:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="3" Grid.Column="1" x:Name="StatusBox" Margin="0,4" Padding="6,4">
            <ComboBoxItem Content="active"/>
            <ComboBoxItem Content="locked"/>
            <ComboBoxItem Content="disabled"/>
        </ComboBox>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Cancel" Width="80" Height="28" Margin="0,0,8,0"
                    IsCancel="True" Click="CancelButton_Click"/>
            <Button Content="Save" Width="80" Height="28"
                    IsDefault="True" Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
